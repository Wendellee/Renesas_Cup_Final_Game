# RA8P1 双核自动巡检项目交接

> 更新时间：2026-08-15
>
> 工程根目录：`D:\Lab\Lab_MCU\Renesas_RA\RenesasCupFinalProjectVehicle_20260815\RA8P1_DualCoreProject`

## 1. 当前结论

除运动与视觉参数仍需实车整定、MPU6050 会受到风扇动力链干扰外，其余主要功能链路均已验证正常喵。

当前不要再排查 M33 是否启动、线程优先级、Video 阻塞、DA16200、遥控器覆盖、IPC 队列或共享 JPEG，这些路径均已有目标板日志证明正常喵。

当前仅保留以下两个主要待办喵。

1. 在安全承托条件下整定前进、左转、吸附和 ROI 参数喵。
2. 从硬件上解决风扇启动导致 MPU6050 I²C 异常的问题，随后重新启用航向闭环喵。

## 2. 已验证正常的功能

- CPU0 与 CPU1 均能正常启动喵。
- M85 摄像头、Gray8 转换、JPEG 编码和共享 SDRAM 发布正常喵。
- CPU1 共享 JPEG 接收、DA16200 联网和前端上传正常喵。
- NRF 命令接收与 NRF 视频发送线程正常喵。
- M85 Navigation Thread 能产生 `STOP/FORWARD/TURN_LEFT` 意图喵。
- M85 到 M33 的 32 位短 IPC 协议正常喵。
- M33 IPC Thread 能执行首次左转意图先停车一帧的策略喵。
- M33 单槽命令邮箱和 Vehicle Thread 消费路径正常喵。
- GPT 风扇、左右车轮的业务调用链正常喵。
- 导航失联超过 500 ms 后停车的保护仍然保留喵。

典型已验证日志如下喵。

```text
[VEHICLE] safe outputs established; waiting for startup gate.
[SHM1] CPU1 ready: base=0x6FFE0000 capacity=69568.
[WIFI] Frontend ready: 192.168.137.1:5000 CID=1.
[VEHICLE] startup gate released.
[VEHICLE] suction starting at 80%.
[VEHICLE] navigation motor output ENABLED.
[VEHICLE][WARN] MPU6050 isolated; straight motion is open-loop.
[NAV IPC] request=FORWARD applied=FORWARD speed=90%.
[NAV IPC] request=TURN_LEFT applied=STOP speed=0%.
[NAV IPC] request=TURN_LEFT applied=TURN_LEFT speed=90%.
[WIFI] Uploaded frame=... JPEG=... crc=... confidence=....
```

## 3. 当前系统数据流

```text
M85 Camera / Encode Thread
        │ Gray8 200x112，只发布最新帧指针
        ▼ 直接任务通知
M85 Navigation Thread
        │ 中央底部 ROI 与连续帧状态机
        ▼ 32 位短 IPC：STOP / FORWARD / TURN_LEFT
M33 IPC Thread
        │ 解码、一帧停车和覆盖写入单槽邮箱
        ▼
M33 Vehicle Thread
        │ 唯一操作底盘业务与 GPT
        ▼
吸附风扇 / 左右车轮

MPU6050 当前被软件隔离，不参与初始化、校准或运行时控制
```

图像载荷继续走共享 SDRAM，导航 IPC 只发送短命令，没有把图像放进 IPC FIFO 喵。

## 4. 当前运动与吸附参数

| 参数 | 当前值 | 配置位置 |
|---|---:|---|
| 导航前进 | 90% | `Renesas_Cup_Vehicle_CPU1/src/ipc_thread_entry.c` |
| 导航原地左转 | 90% | `Renesas_Cup_Vehicle_CPU1/src/ipc_thread_entry.c` |
| 服务层默认前进 | 90% | `Renesas_Cup_Vehicle_CPU1/src/Vehicle/application/vehicle_service.c` |
| 服务层默认转向 | 90% | `Renesas_Cup_Vehicle_CPU1/src/Vehicle/application/vehicle_service.c` |
| 上电吸附风扇 | 80% | `Renesas_Cup_Vehicle_CPU1/src/vehicle_thread_entry.c` |
| NRF `fan=ON` | 80% | `Renesas_Cup_Vehicle_CPU1/src/Radio/application/command_radio.c` |
| 导航命令超时 | 500 ms | `Renesas_Cup_Vehicle_CPU1/src/vehicle_thread_entry.c` |
| 吸附建立等待 | 2000 ms | `Renesas_Cup_Vehicle_CPU1/src/Vehicle/application/vehicle_service.c` |

前进和左转的 90% 只是当前架空联调值，必须根据真实负载、驱动温升和板面摩擦继续整定喵。

风扇 100% 和 95% 曾在目标板表现为不工作，因此已经恢复到此前验证能转的 80% 喵。

吸附力不足不能继续只靠提高软件百分比判断，下一步应测量 GPT7 输出、风扇驱动输入、风扇供电电压和实际电流喵。

## 5. 当前视觉参数

当前 Gray8 图像尺寸为 `200x112`，只使用底部中央窗口作为边缘主判，左右窗口仅用于日志观察喵。

```text
底部 ROI 高度：24 行
灰度阈值：128
中央强危险：d128 <= 65%
中央过渡区：66%～79%
中央安全：d128 >= 80%
启动安全确认：连续 3 帧
过渡区进入危险：连续 2 帧
危险恢复安全：连续 3 帧
所有板边恢复方向：统一左转
```

已在目标板看到 `UNARMED -> SAFE -> CAUTION -> TURN_LEFT_LATCHED -> RECOVERY -> SAFE` 状态变化喵。

中央 `d128` 在 79%～80% 附近出现过误触发，因此 ROI 参数仍属于待整定项，但当前不需要重构状态机喵。

## 6. MPU6050 已确认问题

初始化阶段在风扇启动前，MPU6050 的 `WHO_AM_I`、寄存器配置和 500 次陀螺仪校准曾完整通过喵。

隔离测试中强制车轮停止并保持 `heading=0`，风扇开启后不到 2000 ms，MPU6050 的 `0x3B` 寄存器 14 字节读取仍然失败喵。

第一类错误如下喵。

```text
stage=7
fsp=0
timeout=0
expected=I2C_MASTER_EVENT_RX_COMPLETE
actual=I2C_MASTER_EVENT_ABORTED
addr=0x68
reg=0x3B
len=14
```

该错误说明 `R_IIC_MASTER_Read()` 已经成功启动，FreeRTOS 的 20 ms 等待也没有超时，但 IIC0 硬件随后报告传输中止喵。

再次开启风扇后还出现过以下错误喵。

```text
stage=4
fsp=8
addr=0x68
reg=0x3B
len=1
```

`fsp=8` 是 `FSP_ERR_IN_USE`，说明第一次异常后总线可能仍保持忙状态，下一次寄存器地址发送无法启动喵。

因此已经排除车轮电机、Video 线程、DA16200、遥控器和 M33 调度阻塞，当前故障触发源位于风扇动力链喵。

尚未区分的硬件原因包括 MPU6050 供电跌落、公共地弹噪声、风扇 PWM 或换相干扰、I²C 上拉不合适、走线耦合和回流路径问题喵。

## 7. 当前 MPU6050 软件状态

当前配置位于 `Renesas_Cup_Vehicle_CPU1/src/Vehicle/application/vehicle_service.h` 喵。

```c
#define VEHICLE_MPU6050_ENABLE    (0U)
```

该配置会跳过 MPU6050 初始化、校准和运行时读取，并让直行退化为左右轮等占空比开环输出喵。

所有临时 `TEMP_DIAGNOSTIC` 测试代码已经删除喵。

分阶段 I²C 错误快照仍然保留，但 MPU6050 关闭时不会主动访问 IIC0，也不会影响当前运行喵。

硬件问题解决后，应先在安全条件下把该宏恢复为 `1U`，重新验收初始化、风扇运行时连续读取和首次直行闭环喵。

## 8. 新对话第一步

如果当前优先目标是完成演示，应先在车轮架空、板外同高承托或可靠托车条件下整定以下参数喵。

1. 确认 90% 前进时左右轮都能起转且方向正确喵。
2. 确认 90% 左转时左轮反转、右轮正转喵。
3. 调整前进和转向到能够可靠起转但不过度冲击的最低占空比喵。
4. 保持风扇 80%，测量实际吸附效果和驱动温升喵。
5. 最后再微调中央 ROI 阈值，避免把电机问题与视觉参数问题混在一起喵。

如果当前优先目标是恢复 MPU6050，应先用示波器同步观察以下信号喵。

1. MPU6050 本地供电对本地地喵。
2. SDA 对 MPU6050 本地地喵。
3. SCL 对 MPU6050 本地地喵。
4. 风扇供电或驱动开关节点喵。

IIC0 实际速率约为 97.8 kHz，SDA 和 SCL 空闲时都应保持高电平，标准模式上升时间应不超过约 1 us 喵。

不要先加入无限重试或屏蔽错误，必须先确认供电跌落、NACK、线路被拉低或其他硬件原因喵。

## 9. 安全边界

当前 MPU6050 被隔离，直行没有航向修正，不能在无保护的垂直板面运行喵。

90% 车轮占空比和 80% 风扇占空比只完成了功能联调，尚未完成持续电流、驱动温升和机械冲击验收喵。

当前 `vehicle_service_emergency_stop()` 会同时停止车轮和吸附风扇，这对依靠负压附着的车辆可能不安全喵。

后续应把“运动故障停车但保持吸附”和“明确全系统关闭”拆成两种安全动作喵。

在完成该安全策略前，不要进行无承托的垂直板面测试喵。

## 10. 关键文件

| 文件 | 用途 |
|---|---|
| `自动巡检两天实施计划书.md` | ROI 数据、架构决策和历史实施记录 |
| `Renesas_Cup_Vehicle_CPU0/src/navigation_thread_entry.c` | M85 ROI 状态机与导航 IPC 发送 |
| `Renesas_Cup_Vehicle_CPU0/src/encode_thread_entry.c` | Gray8 生成后发布最新导航帧 |
| `Renesas_Cup_Vehicle_CPU1/src/ipc_thread_entry.c` | 导航消息解码、停车一帧和速度参数 |
| `Renesas_Cup_Vehicle_CPU1/src/vehicle_thread_entry.c` | 底盘线程、启动风扇、命令超时和日志 |
| `Renesas_Cup_Vehicle_CPU1/src/Vehicle/application/vehicle_service.h` | MPU6050 启用开关 |
| `Renesas_Cup_Vehicle_CPU1/src/Vehicle/application/vehicle_service.c` | 吸附、开环或闭环直行与紧急停机 |
| `Renesas_Cup_Vehicle_CPU1/src/Vehicle/platform/fsp_vehicle_actuators.c` | GPT 风扇和车轮适配层 |
| `Renesas_Cup_Vehicle_CPU1/src/Vehicle/platform/fsp_vehicle_i2c.c` | IIC0 适配层与错误快照 |
| `Renesas_Cup_Vehicle_CPU1/src/Vehicle/device/mpu6050.c` | MPU6050 初始化、校准和采样 |

## 11. 构建状态

CPU1 最新版本已执行 `mingw32-make -j8 all` 并成功生成 ELF 与 SREC 喵。

```text
CPU1 text = 60030
CPU1 data = 228
CPU1 bss  = 25360
CPU1 srec = Renesas_Cup_Vehicle_CPU1/Debug/Renesas_Cup_Vehicle_CPU1.srec
```

## 12. 不要重复或误操作

- 不要再调整线程优先级来解决 MPU6050 故障喵。
- 不要新增 EventGroup 或 Queue 来解决当前底盘问题喵。
- 不要修改 DA16200 或 Video 链路，它们当前正常喵。
- 不要重新怀疑 CPU1、IPC Thread 或 Vehicle Thread 没有运行喵。
- 不要把软件隔离 MPU6050 当作硬件问题已经修复喵。
- 不要手工修改 `ra_gen`、`configuration.xml`、链接脚本或 FSP 生成文件喵。
- 不要使用 `git reset --hard`、`git checkout --` 或大范围清理命令喵。
