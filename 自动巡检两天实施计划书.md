# RA8P1 光伏板小车简化自动运动计划书

## 1. 目标与边界

本项目的主任务仍然是 YOLO 故障识别、识别结果上传和实时图传，运动功能只实现能够演示的最小闭环喵。

自动模式只做以下流程喵。

```text
自动启动
   ↓
低速前进
   ├── 发现板边 → 立即停止车轮 → 向板内左转或右转 → 继续前进
   ├── YOLO发现故障 → 向故障框相反方向转一下 → 继续前进
   └── 图像/IPC异常 → 停止车轮并等待恢复
```

本阶段明确不做弓字覆盖规划、全局地图、里程计、SLAM、定角闭环转弯、停车距离计算和复杂避障轨迹喵。

由于车辆采用负压吸附，车轮停止响应可直接作为边缘保护动作，本计划不再建立制动距离模型喵。

## 2. 查阅代码后的结论

### 2.1 现有灰度图传必须直接复用

M85 的 `Encode Thread` 已经每 100 ms 获取一帧最新的 `1024×600 RGB565` 图像，并转换成 `200×112 Gray8` 灰度图喵。

现有调用链如下喵。

```text
camera_completed_frame_get()
        ↓
video_rgb565_to_gray8()
        ↓
g_video_gray[200 × 112]
        ↓
ImageJpeg_EncodeGray8()
        ↓
shared_video_cpu0_publish()
```

相关代码位于以下文件喵。

- `Renesas_Cup_Vehicle_CPU0/src/encode_thread_entry.c` 喵。
- `Renesas_Cup_Vehicle_CPU0/src/ImageUpload/Image_JPEG_Encoder.c` 喵。
- `Renesas_Cup_Vehicle_CPU0/src/IPC/shared_jpeg_cpu0.c` 喵。

因此，边缘检测不应再次从 RGB565 生成另一份图像，也不应改变当前灰度 JPEG 图传协议喵。

需要注意的是，现有 `g_video_gray` 是 8 位灰度图，不是每像素只有 0/1 的二值图喵。

导航线程只需对该灰度图的底部区域做一次很轻的阈值二值化，即可得到板面与板外区域喵。

### 2.2 AI 结果已经有线程安全快照

AI Thread 已通过 `ai_inference_result_publish()` 发布最新 YOLO 结果，显示线程也已经通过 `ai_inference_result_get_latest()` 读取该快照喵。

因此，不需要把运动状态机塞入 `ai_thread_entry.c`，新的导航线程直接读取现有 YOLO 快照即可喵。

### 2.3 M33 必须继续独占电机硬件

现有 M33 架构规定只有 `Vehicle Thread` 可以操作 GPT 车轮、负压风机和相关底盘硬件，这个边界是正确的，必须保留喵。

M85 或 M33 的 IPC/NRF 接收线程都只能提交命令，不能直接写电机 PWM 喵。

## 3. 推荐的最简线程分工

建议在 M85 新增一个轻量 `Navigation Thread`，不要继续扩大 AI Thread 喵。

| 模块 | 只负责什么 |
|---|---|
| `AI Thread` | YOLO 推理并发布最新检测框喵。 |
| `Encode Thread` | RGB565 转 Gray8、灰度 JPEG 编码和图传发布喵。 |
| `Navigation Thread` | 灰度 ROI 边缘判断、读取 YOLO 快照、生成四类运动命令喵。 |
| `CPU0 IPC` | 只向 M33 发送很短的导航命令喵。 |
| `CPU1 IPC/NRF` | 接收自动或手动命令并送入单槽最新命令邮箱喵。 |
| `Vehicle Thread` | 唯一执行前进、转弯、停止和负压控制的线程喵。 |

建议数据流如下喵。

```text
VIN最新RGB565帧
      ↓
Encode Thread：生成现有200×112 Gray8
      ├──────────────→ 灰度JPEG编码 → 现有实时图传
      │
      └─任务通知────→ Navigation Thread
                         ├─底部ROI二值化 → 边缘方向
                         ├─读取最新YOLO快照 → 故障框方向
                         └─生成 FORWARD / LEFT / RIGHT / STOP
                                           ↓ 短IPC
                                      M33 Vehicle Thread
                                           ↓
                                        GPT车轮
```

`Encode Thread` 当前优先级为 4，`AI Thread` 当前优先级为 5，建议 `Navigation Thread` 使用优先级 6，栈先配置为 2 KiB，并用栈水位实测后再调整喵。

`Encode Thread` 应在 Gray8 转换完成后立刻通知导航线程，然后再进行 JPEG 压缩喵。

导航线程被唤醒后只读取底部 ROI、做整数统计并立即返回，不进行 JPEG、日志刷屏或任何阻塞操作喵。

在这种优先级关系下，导航线程会先完成一次安全判断，随后 `Encode Thread` 才继续压缩同一帧 Gray8，既避免整帧复制，也避免边缘判断被 JPEG 编码拖后喵。

正式实现时应在 FSP 中添加线程并重新生成，不手工修改 `ra_gen` 文件喵。

## 4. 边缘检测的简化方案

### 4.1 只检查图像下方

第一版只检查 `200×112 Gray8` 图像最下方约 20～30 行，并把它分成左、中、右三个窗口喵。

```text
┌────────────────────────────┐
│                            │
│       不参与边缘判断       │
│                            │
├─────────┬────────┬─────────┤
│ 左窗口  │ 中窗口 │ 右窗口  │  ← 底部20～30行
└─────────┴────────┴─────────┘
```

每个窗口只统计“像板面”的像素数量，不运行轮廓跟踪、直线拟合或透视变换喵。

第三组实测确认中央窗口适合当前正前方边缘主判，左右窗口受板面纹理和视角影响较大，当前只保留为辅助日志喵。

阈值和板面黑白极性不能只凭代码猜测，必须把车放在真实光伏板上，根据 Gray8 实际数值标定喵。

### 4.2 简单决策表

| 中央窗口连续意图 | 动作 |
|---|---|
| 启动尚未连续确认安全 | 保持停止，不产生转向动作喵。 |
| `d128 >= 80%` | 视为安全候选，满足连续确认后允许低速前进喵。 |
| `66% <= d128 <= 79%` | 立即停止观察，连续两帧后锁存左转意图喵。 |
| `d128 <= 65%` | 立即停车并锁存左转意图喵。 |
| 图像无效 | 保持停止，不产生转向动作喵。 |

进入危险状态只需一帧确认，以尽快停车喵。

从危险恢复为安全时建议连续得到 2～3 帧安全结果再恢复前进，防止阈值附近抖动导致车轮反复启停喵。

## 5. YOLO 故障后的简化动作

导航线程读取最新 YOLO 快照，并只使用最高置信度故障框的横向中心位置喵。

| 故障框位置 | 动作 |
|---|---|
| 图像左半边 | 右转一下，然后继续前进喵。 |
| 图像右半边 | 左转一下，然后继续前进喵。 |
| 图像中间且左右均可走 | 使用上一次转弯的相反方向，避免一直偏向同一侧喵。 |
| 同时检测到板边 | 忽略 YOLO 转向，优先执行板边停车与转向喵。 |

同一个故障持续出现在画面中时不能每帧重复触发转弯，因此需要一个很小的触发锁存喵。

触发一次后，只有故障连续消失若干帧或经过短暂冷却时间，才允许再次触发喵。

## 6. M85 到 M33 的最小命令

IPC 只发送短控制信息，不发送图像数据喵。

```text
NAV_FORWARD
NAV_TURN_LEFT
NAV_STOP
```

每条命令附带 8 位帧序号和 8 位校验字节，M33 收到后记录本地 tick，用于判断视觉链路是否停止更新喵。

M33 超过约 500 ms 没收到新的自动命令时停止车轮，避免摄像头、导航线程或 IPC 卡住后继续前进喵。

该导航消息必须使用独立 IPC 消息类型，不能占用或破坏现有故障 JPEG、实时视频槽位及回执状态机喵。

## 7. M33 的最小自动状态机

M33 不需要复杂路径规划，只需以下三个动作状态喵。

```text
STOP
FORWARD
TURN_LEFT
```

执行规则如下喵。

1. 收到 `NAV_STOP` 或自动命令超时，立即执行 `STOP` 喵。
2. 首次收到 `NAV_TURN_LEFT` 时先向 `Vehicle Thread` 提交一帧 `STOP`，下一帧仍为危险才提交 `TURN_LEFT` 喵。
3. M85 危险意图锁存期间持续发送 `TURN_LEFT`，中央窗口连续 3 帧安全后才恢复 `FORWARD` 喵。
4. 所有电机动作仍由 `Vehicle Thread` 调用 `vehicle_service` 完成喵。

第一版不用 MPU6050 做 90° 定角控制，也不要求每次转弯角度完全一致喵。

只需在真实负压和低速条件下，把左、右固定转向时间分别调到“转完后车头仍朝向板内且能继续前进”喵。

## 8. 手动与自动切换的最简保留方案

自动功能验收后，如果还有时间，再接遥控器模式切换喵。

仲裁顺序保持简单喵。

```text
边缘危险 / 图像失效 / 命令超时
              ↓ 最高优先级
             STOP

MANUAL模式 → M33 nRF命令控制前后左右
AUTO模式   → M85导航命令控制前进和转向
```

遥控器下发手动模式时，M33 应先停车、切换为 `MANUAL`，再执行 nRF 方向命令喵。

遥控器按下 Auto 时，M33 应先停车、清除上一次手动方向、切换为 `AUTO`，等待一条新的 M85 导航命令后继续自动前进喵。

即使处于手动模式，边缘危险也建议保留为强制停车保护，因为防跌落比手动方向命令优先级更高喵。

如果两天内遥控链路来不及验证，手动模式可以暂时不做，不影响自动演示闭环喵。

## 9. 验收标准

### 9.1 自动边缘保护

- 小车低速前进时，遇到板边后先停车一帧，随后统一左转喵。
- 左转期间不因单帧画面变化而解锁，中央窗口连续 3 帧安全后恢复前进喵。
- 图像无效、阈值结果无法判断或 IPC 超时不会继续盲目前进喵。
- 在平放板面或有承托保护的条件下，每个方向至少重复 10 次不发生越界喵。

### 9.2 YOLO 简单反应

- 故障框在左侧时只触发一次右转，故障框在右侧时只触发一次左转喵。
- 转向结束后自动恢复低速前进喵。
- 同一故障持续存在时不会每 100 ms 重复触发转弯喵。
- YOLO 与边缘同时触发时，边缘命令覆盖 YOLO 命令喵。

### 9.3 不影响主任务

- 原有灰度 JPEG 图传仍能连续工作喵。
- 原有故障 JPEG 上传与 IPC 回执不被导航消息破坏喵。
- AI Thread 仍只负责推理与结果发布，没有加入电机状态机喵。
- M33 的 GPT 电机硬件仍只有 `Vehicle Thread` 一个业务所有者喵。

## 10. 实施边界

当前已将 Gray8 ROI 观测与连续意图判断从 M85 `Encode Thread` 分离到正式 `Navigation Thread` 喵。

用户已通过 FSP 新增并生成 `Navigation Thread`，业务代码不再手工修改 `ra_gen`、`configuration.xml`、链接脚本或共享 SDRAM 分区喵。

分类结果已接入短 IPC 和 M33 命令邮箱，但首次联调使用 `VEHICLE_NAV_MOTOR_OUTPUT_ENABLE=0` 强制车轮停车，只验证日志和失联保护喵。

## 11. 2026-08-15 Gray8 ROI 实测数据

原始数据保存在 `C:\Users\lingk\Desktop\转向数据.txt`，以下记录用于工程决策的稳定范围和代表帧喵。

`d128` 表示窗口内灰度值小于 128 的像素比例，数值越高越像当前实测中的深色光伏板，数值越低越像明亮的板外区域喵。

| 实测场景 | 平均灰度 L/C/R | `d128` L/C/R | 工程结论 |
|---|---:|---:|---|
| 画面稳定处于板内 | 约 `62/59/66` | 约 `86/88/84%` | 三个窗口均可作为板面可信基线喵。 |
| 左转安全极限 | 约 `182/189/160` | 约 `37/33/48%` | 三个窗口均已明显偏离板内基线，右侧保留板面特征相对更多喵。 |
| 右转安全极限 | 约 `239/249/233` | 约 `6/0/10%` | 三个窗口几乎全部失去板面特征，只允许停车喵。 |
| 边沿右转过程 `frame=375` | `215/147/94` | `19/53/76%` | 右侧仍像板面，应判断左侧危险并向右恢复喵。 |
| 边沿左转过程 `frame=374` | `78/107/227` | `82/70/0%` | 左侧仍像板面，应判断右侧危险并向左恢复喵。 |

### 11.1 候选分类参数

第一版使用以下候选参数，只用于 RTT 分类验证喵。

```text
上电后中央窗口连续3帧 d128 >= 80% → 完成安全预热并进入 SAFE
中央窗口 d128 <= 65% → 强危险样本，一帧立即锁存
65% < 中央窗口 d128 < 80% → 过渡样本，连续2帧后锁存
危险锁存后中央窗口连续3帧 d128 >= 80% → 解除锁存并恢复 SAFE
```

当前分类规则如下喵。

```text
启动未完成连续3帧安全预热         → UNARMED / STOP_WAIT_SAFE
稳定板面                         → SAFE / FORWARD_ALLOWED
第一帧中央过渡样本               → CAUTION / STOP_MONITOR
中央强危险或连续2帧过渡          → TURN_LEFT_LATCHED / STOP_THEN_LEFT
危险锁存后的第1～2帧恢复候选     → RECOVERY / STOP_CONFIRM_SAFE
中央连续3帧恢复候选              → SAFE / FORWARD_ALLOWED
```

危险锁存后不再根据转向过程中的瞬时左右画面改变转向意图，所有板边统一向左恢复喵。

进入正式电机控制后，M33 首次收到左转意图先停车一帧，下一帧危险仍锁存才执行左转，并在导航命令超过 500 ms 未更新时停车喵。

### 11.2 几何与安全修正

摄像头位于小车前方中央，不能要求车辆为了构造单侧板外画面而静止移动到可能跌落的位置喵。

后续不再要求无承托条件下补采此类静态板边数据，所有动态分类和电机验收必须在板面平放、板外同高度承托、安装安全绳或有人可靠托车的条件下进行喵。

### 11.3 第二组连续转向数据与决策

第二组原始数据保存在 `C:\Users\lingk\Desktop\转向数据2.txt` 喵。

左转过程中 `frame=7328` 曾单帧进入 `SAFE`，但随后再次进入危险，直到 `frame=8077` 后才连续稳定为 `SAFE` 喵。

右转过程中 `frame=10992` 也曾单帧进入 `SAFE`，下一帧立即变为三个窗口均不可信，直到 `frame=11367` 后才连续稳定为 `SAFE` 喵。

该现象证明转向时不能使用单帧 `SAFE` 解除动作，也不能逐帧切换左右转向意图喵。

右转过程中还长期出现 `d128=95/94/72%`，证明旧规则对单个侧窗过度敏感喵。

第二组曾暂定降低侧窗恢复阈值，但第三组进一步证明安全区右窗口可长期低至约 60%，因此该临时方案已被中央窗口主判规则取代喵。

为降低两天演示任务的控制复杂度，自动边沿恢复正式确定为统一左转，右转识别与右转动作退出当前实施范围喵。

连续意图判断只保存危险锁存、连续过渡帧数和连续恢复帧数，不保存或复制历史图像喵。

当前 ROI 每帧仅统计底部 4800 个 Gray8 像素，相比现有 RGB565 缩放、滤波和 JPEG 编码负载很小，因此正式线程第一版暂不使用 Helium 喵。

RTT 在意图状态变化时立即输出，意图不变时每 10 个新帧输出一次，兼顾状态可观察性与日志开销喵。

### 11.4 第三组中央窗口标定与规则修正

第三组原始数据保存在 `C:\Users\lingk\Desktop\数据三.txt` 喵。

安全静止阶段中央窗口 `d128` 稳定在约 `90%～92%`，而右窗口只有约 `60%～62%`，证明单个侧窗不能作为全局危险的一票否决条件喵。

车辆到达转向极限并等待转向时，中央窗口稳定在约 `54%～55%`，与安全静止阶段存在约 35 个百分点的明确差值喵。

左转恢复过程中中央窗口依次从约 `54%` 上升到 `68%`、`79%`、`88%` 及更高值，能够形成危险、过渡和安全三个区间喵。

因此第一版正式改为中央窗口主判，左右窗口只保留为 RTT 辅助观测数据，不参与危险锁存或恢复解锁喵。

中央窗口候选阈值确定为 `<=65%` 强危险、`66%～79%` 过渡、`>=80%` 安全，并继续使用危险一帧进入、过渡两帧进入、安全三帧恢复的非对称时序喵。

## 12. 2026-08-15 M85→M33 正式接入记录

M85 `Encode Thread` 在 Gray8 转换完成后只发布最新帧指针并向 `Navigation Thread` 发送直接任务通知，不复制图像、不等待队列喵。

`Navigation Thread` 使用用户已生成的优先级 6、2 KiB 静态线程，只保存危险锁存与 3 个连续帧计数器，不保存历史图像喵。

32 位 IPC 命令格式为 `magic(0xA7) | action | sequence | checksum`，其中动作只有 `STOP/FORWARD/TURN_LEFT`，不占用共享 SDRAM 图像数据槽喵。

CPU1 IPC 回调只鉴别消息、保存最新值并唤醒 IPC Thread，命令解码、日志和邮箱提交均在任务上下文完成喵。

M33 继续复用现有长度 1 的 `vehicle_command_mailbox`，由 `xQueueOverwrite()` 保留最新命令，`Vehicle Thread` 仍是 GPT 和 `vehicle_service` 的唯一业务所有者喵。

本链路不新增 EventGroup 或 Queue，因为 M85 帧交接是单生产者、单消费者的“最新值”语义，任务通知已足够；M33 输出端已有可覆盖的单槽邮箱喵。

调参宏集中在 `navigation_thread_entry.c`、`ipc_thread_entry.c` 和 `vehicle_thread_entry.c` 顶部，并已附中文注释喵。

为避免上电时从未知画面直接产生左转动作，启动阶段增加连续三帧中央安全确认，在确认完成前只允许 `STOP_WAIT_SAFE` 喵。

## 13. 2026-08-15 底盘隔离验收更新

分阶段 I²C 日志已经确认，风扇开启且车轮强制停止时，MPU6050 的 `0x3B` 寄存器 14 字节读取仍会收到 `I2C_MASTER_EVENT_ABORTED` 喵。

错误后再次访问可能在寄存器地址 TX 启动阶段返回 `FSP_ERR_IN_USE=8`，说明总线没有正常恢复为空闲状态喵。

当前软件临时使用 `VEHICLE_MPU6050_ENABLE=0U` 隔离 MPU6050，直行退化为左右轮等占空比开环控制，不能用于无保护的垂直板面运行喵。

当前验收参数为前进 90%、左转 90%、启动吸附 80% 和 NRF 风扇开启 80% 喵。

目标板实测风扇 100% 和 95% 请求时均未正常工作，因此吸附参数恢复到此前已验证能转的 80%，后续必须测量 GPT7 输出和风扇驱动端电压再决定极性或占空比修正喵。

下一步先在架空或同高承托条件下确认前进与左转输出，再使用示波器检查风扇启动瞬间的 MPU6050 供电、SDA 和 SCL，软件隔离不能替代硬件修复喵。
